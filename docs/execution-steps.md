# 执行步骤（开发推进计划）

> 跟随 [DESIGN.md](../DESIGN.md) 第 7 节「里程碑（建议实现顺序）」。
> **推进纪律：** 每个里程碑结束时，工程必须能构建、能运行、能人工验收该步功能；验收通过才进入下一个。每步完成后在 `devlog/` 追加日志。
> 未知问题（依赖版本冲突、权限策略等）先小步验证，再扩展，不一次性做大量改动。

## 阶段 0 — 工程化骨架（本阶段，已部分完成）

- [x] 建立 `devlog/`、`docs/`，编写 [CLAUDE.md](../CLAUDE.md)。
- [x] 制定推进计划（本文件）。
- [x] **入口**：接入依赖（Media3 1.5.1、Room+KSP、navigation-compose、lifecycle-viewmodel-compose），确认构建通过。

## 里程碑 1 — 骨架（已完成，2026-09-03 验收通过）

- [x] 补依赖：Media3 1.5.1（exoplayer/ui/common）、Room（runtime/ktx/compiler，KSP）、navigation-compose、lifecycle-viewmodel-compose。
- [x] 配 KSP 插件；版本目录 `libs.versions.toml` 增项。
- [x] 建 `AppDatabase`：Video / Segment / RecordingChunk 三张表 + DAO（先接入，字段按 DESIGN 第 3 节）。
- [x] 建导航：单 Activity，Compose Navigation，`HomeScreen` → `PlayerScreen`（PlayerScreen 先占位）。
- [x] **验收**：构建通过；主页显示视频库（空态 + 本地添加按钮入口可点通到占位页）。

## 里程碑 2 — 播放器基础（已完成，2026-09-03 验收通过）

- [x] 主 Player 播放本地视频；`PlayerView` 渲染。
- [x] 时间轴（进度条 + 拖动定位）。
- [x] 播放控制：播放/暂停、倍速 0.5–1.0 步进 0.1。
- [x] 循环播放（先整视频循环，块循环在分段后接入）。
- [x] 全屏：immersive 全屏 + 屏幕常亮；单击唤出控件层、单击中间或超时隐藏。
- [x] 横竖屏 FIT 缩放（RESIZE_MODE_FIT）；横屏视频自动锁横屏（用户变更，见 technical.md 差异节）。
- [x] **验收**：进入播放器能播、能拖、能调倍速、能全屏切换控件层；横竖屏都不拉伸。（2026-09-03 用户验收通过，含自动横屏与右下角倍速按钮）

## 里程碑 3 — 分段（已完成，2026-09-03 验收通过）

- [x] 分割点模型与 UI：时间轴手柄，插入 / 删除 / ±10ms·±100ms 微调。
- [x] 段数据落库：Segment 增删改；删除分割点合并相邻段。
- [x] 块选择：连续段选择条，起始/结束两个手柄（实现为点选扩展/收起，见 devlog）。
- [x] 块循环播放接入（选中块 + 循环开 → 播放限制在块内循环）。
- [x] **验收**：能切段、能微调、能选连续段成块，数据正确存储。（2026-09-03 用户验收通过，含分段编辑开关式 UI）

## 里程碑 4 — 手势（已完成，2026-09-03 验收通过）

- [x] 手势层：长按右侧=0.5x、长按左侧=2x 模拟倒放、横滑快进快退 + 「±N 秒」提示条。
- [x] 避开控件区域；录制态整体禁用（里程碑 5 已接入录制态屏蔽）。
- [x] **验收**：三种手势在画面区生效、控件不受影响、松开恢复。（2026-09-03 用户验收通过）

## 里程碑 5 — 录音（已完成，2026-09-04 验收通过）

- [x] `RECORD_AUDIO` 权限与运行时申请。
- [x] MediaRecorder 录 AAC→m4a 到私有目录。
- [x] 块录音流程：视频块 @1x 播一遍、手动/到块尾停止、读回实际时长 recordedMs。
- [x] RecordingChunk 入库（同块重录替换；与新块范围重叠的旧片段一并替换）。
- [x] **验收**：对某块录音并停止，能生成音频文件与数据记录，主页出现「有配音」标记。（2026-09-04 用户验收通过）

## 里程碑 6 — 配音回放（已完成，2026-09-05 验收通过）

- [x] 配音 Player（audio-only），按片段绝对时间构造配音源（Clipping + Silence 补位，覆盖整视频的顺序播放列表，见 technical.md）。
- [x] 双 Player 同步（主 Player 位置不连续即时对齐 + ticker 纠偏）；倍速一致。
- [x] 音量滑条×2 与配音开关。
- [x] **验收**：选中已录块，循环回放时配音与原视频同步；可独立调音量 / 关闭配音。（2026-09-05 用户验收通过；期间修复初始化 NPE 与配音轨构建竞态两个 bug，见 devlog）

## 里程碑 7 — 网络视频（已完成，2026-09-05 验收通过；形态变更见 technical.md 差异节）

- [x] ~~URL 输入 → 抓元信息 → 下载到本地~~：曾实现，2026-09-05 用户确认由 m4s 导入替代后**移除**。
- [x] 导入 B 站客户端缓存 m4s 文件对（video.m4s + audio.m4s），remux 合并为普通本地视频入库。
- [x] **验收**：m4s 导入后画音正常，参与分段配音。（2026-09-05 用户验收通过）

## 里程碑 8 — 打磨

- [ ] 封面提取（中间帧→缩略图）。
- [ ] 横竖屏细节打磨、中文文案统一、UI 风格统一。
- [ ] 边界处理：删除视频级联清 Segment/RecordingChunk/文件；录音中断恢复；应用后台/切前台恢复播放状态。
- [ ] **验收**：整体走查一遍全部功能，无明显缺陷。

## 仓库根目录标准文件

- `DESIGN.md` — 方案设计（定稿，权威）
- `CLAUDE.md` — 工作说明与标准路径
- `devlog/` — 开发日志（含 INDEX.md 与按日文件）
- `docs/` — 开发文档（requirements / technical / coding-standard / execution-steps）
