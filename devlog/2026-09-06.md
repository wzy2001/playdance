# 2026-09-06

## 已完成

- [x] **里程碑 8 步骤 1：封面缩略图，代码完成、构建通过**。
  - 新增 `ThumbnailGenerator`（中间帧 → ≤640px JPEG 80 → `filesDir/thumbnails/`，失败返回 null 不影响导入）；`LocalVideoImporter` / `M4sVideoImporter` 导入时生成并写 `thumbnailPath`。
  - 存量视频补齐：`VideoDao` 新增 `@Update update(video)` 与 `getMissingThumbnails()`；`HomeViewModel.init` 后台串行补齐（源文件缺失跳过，幂等）。**关键坑**：更新 videos 行必须用 `@Update`——REPLACE insert 在开外键时先 DELETE 再 INSERT，会级联删光该视频的分段与配音（已记 technical.md）。
  - 主页 `VideoCard` 改左图右文（120×68dp 圆角 Crop 缩略图 + 标题两行截断 + 时长），不引 Coil：`VideoThumbnail` composable 用 `produceState` + `BitmapFactory` 两遍解码后台加载，null/解码失败显示灰底占位图。
  - 顺带修 bug：本地导入目标文件名加时间戳前缀，防同名文件二次导入覆盖前一个视频的源文件。
- [x] **里程碑 8 步骤 2：删除级联清理 + 确认对话框，代码完成、构建通过**。
  - `HomeViewModel.deleteVideo`：删行前收集视频/缩略图/全部 chunk 音频路径 → 删 DB 行（级联删子表）→ IO 线程逐个删文件（单个失败不影响其余）。顺序上先 DB 后文件，坏状态只可能是孤儿文件（有清扫兜底）。
  - 主页删除改为先弹中文确认对话框（`delete_confirm_title` / `delete_confirm_message`，说明不可恢复）；顺带删掉 `HomeScreen.kt` 未使用 import `rememberModalBottomSheetState`。
- [x] **里程碑 8 步骤 3：生命周期处理，代码完成、构建通过**。用户已确认语义：录音中切后台=停止并保存已录部分；切后台暂停播放、回前台保持暂停；返回键退出仍丢弃录音。
  - `PlayerViewModel.onHostStopped()`：录音中先 `stopRecording()`（既有保存入库语义）再 `player.pause()`（配音 Player 由 200ms ticker 镜像跟随暂停）。
  - `PlayerScreen` 新增 `DisposableEffect`：`LifecycleEventObserver` 注册在 **Activity** lifecycle（不能用 NavBackStackEntry 的 LocalLifecycleOwner，否则按返回也触发 ON_STOP 与 onCleared 竞态）；仅在 `ON_STOP && !activity.isChangingConfigurations` 时触发（旋转、横屏视频进页面的方向锁定都会重建 Activity 走 ON_STOP，必须排除,否则横屏视频一进页面就被误暂停）。
  - 孤儿录音清扫：`RecordingChunkDao.getAllAudioPaths()`;`HomeViewModel` 启动时（AtomicBoolean 保证进程内一次）删 `recordings/` 下无行引用且 >60s 未修改的文件（年龄阈值防与录音停止后的异步落库竞态）。
- [x] **里程碑 8 步骤 4：文案与 UI 走查完成**。Compose 层无硬编码文案（全部走中文字符串资源）、IconButton contentDescription 齐全（装饰图为 null）、对话框行为合理。记录的保留决策：importer 异常消息为硬编码中文（作为导入失败对话框详情展示,工具类无 Context,保留）；`RecordingRed` 为刻意的录音语义色（保留）。
- [x] 同步文档：`docs/technical.md` 新增「打磨（里程碑 8 实现决策）」一节（缩略图、@Update 陷阱、删除级联、生命周期挂钩、孤儿清扫、导入文件名）。
- [x] **用户追加的两个主页 UI 改动，代码完成、构建通过**。
  - 「有配音」文字标签改为节拍图标 `Icons.Filled.GraphicEq`（primary 色、20dp，contentDescription 保留「有配音」），功能不变。
  - 卡片右侧删除按钮改为三点菜单（`MoreVert` + `DropdownMenu`）：「重命名」与「删除」。删除走原确认框流程不变；重命名弹对话框（预填当前名，空白禁用确认），`HomeViewModel.renameVideo` 用 `@Update` 只改 title（沿用防 REPLACE 级联陷阱），文件名不动。
  - 新增字符串：`video_more_actions` / `rename_video` / `rename_dialog_title` / `rename_title_label`。

## 待验收（用户在 AS + Pixel_8 模拟器人工测试）

- [ ] **步骤 1（缩略图）**：①新导入本地视频与 m4s 各一个，主页卡片左侧出现中间帧缩略图；②旧视频先显示灰底占位图，稍候自动变为缩略图（Room Flow 自动刷新）；③Device Explorer 可见 `files/thumbnails/*.jpg`；④**补齐后旧视频的分段与配音完好**（验证没触发级联删除）；⑤滚动列表流畅、图文对应不错位。
- [ ] **步骤 2（删除）**：①点三点菜单选「删除」弹中文确认框（含视频名），取消无变化；②确认后卡片消失，Device Explorer 确认该视频的 mp4、缩略图 jpg、全部录音 m4a 均已删除，Database Inspector 三张表行全消失；③删一个源文件已被手动删掉的视频不崩溃。
- [ ] **步骤 3（生命周期）**：①播放中按 Home 切后台：声音立即停止；切回后停在原位置、保持暂停，点播放继续；②录音中切后台：回到应用后录音已停止，该块显示有配音，回放能听到切走前录的内容；③**横屏视频进播放页正常自动播放，不被误暂停**；④竖屏视频播放中旋转设备不中断；⑤录音中按返回键退出仍是丢弃；⑥（可选）往 `recordings/` 放一个无引用的 m4a，等 1 分钟后重启 App 进主页,文件被清掉,已有配音不受影响。

- [ ] **主页 UI 改动**：①有配音的视频卡片显示节拍图标（不再是文字）；②三点菜单展开出现「重命名」「删除」两项；③重命名后卡片标题即时刷新，视频的分段与配音完好（验证没触发级联删除），播放页标题一致；④空白名称时「重命名」按钮不可点。

## 待办（下一步）

- [ ] 用户验收上述三组清单;全部通过后整体走查一遍全部功能（里程碑 8 验收条件），更新 execution-steps.md 勾选与 HANDOFF.md 状态。

## 备注

- 构建均通过（assembleDebug）;仅存的编译警告是已知的 `unappliedRotationDegrees` 弃用（HANDOFF 记录可忽略）。
- 本里程碑无数据库结构变更，无需 Migration。
