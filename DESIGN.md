# 跳舞练习视频播放器 —— 方案设计文档

> 状态：**定稿。** 功能与技术方案已与用户逐条确认。可按本文档开始实现。
> 技术栈：Kotlin + Jetpack Compose + Media3 (ExoPlayer) 1.5.1，Room 本地数据库，全部数据存 App 私有目录。

---

## 1. 一句话定位

一个本地优先的视频播放器，核心是**辅助跳舞练习**：把任意视频用「分割点」切成若干连续的段，选中若干连续段作为「块」，整块循环播放；并能**分块录制配音**，之后把配音与原视频**同步叠加**一起播放。

---

## 2. 功能清单（已与用户逐条确认）

### 2.1 视频来源
- **本地视频**：从系统文件选择器导入，复制到 App 私有目录。
- **网络视频**：主页「+」→ 输入 URL → 显示元信息（标题 / 时长 / 封面）→ 提供「下载到本地」按钮。下载为**原样保存**（不转码），下载后就是普通本地视频。
- 所有视频、配音、数据库全部位于 **App 私有目录**（`filesDir` 与数据库），系统文件管理器不可见、不随文件观察器变动。

### 2.2 主页（视频库）—— 首页
- 顶部或悬浮 **「+」按钮**。点按后弹出两个选项：**添加本地视频**、**输入 URL**。
- 每条视频显示：**标题 / 封面缩略图 / 时长 / 是否有配音**。
- 点击一条视频 → 进入**播放器页**。

### 2.3 播放器页（合并界面）
播放、分段、配音、循环都在**同一个界面**完成，共享同一份时间轴状态，杜绝双页面不同步。
界面构成：
- **视频画面**（ExoPlayer 渲染）。
- **时间轴**：显示分割点标记、当前进度、拖动定位。
- **段选择**：以「块」为单位选择若干**连续**段（可只选一段；相邻段自动连成连续块，不支持跳段选择）。
- **播放控制**：播放/暂停、进度、循环开关、**倍速 0.5–1.0（步进 0.1）**、**配音开关**、**原声音量滑条 + 配音音量滑条**。
- **分割点编辑**：插入、删除、**精确微调（± 毫秒）**。
- **录制按钮**：进入录音状态。

### 2.4 分段 / 循环
- 一个视频被任意数量的**分割点**切成 **N+1** 个连续、不重叠的段。
- 分割点可**增删**、可**精确微调**（按毫秒步进，对齐到拍子）。
- 选中任意**连续**若干段作为「块」，**整块无限循环**播放。无「循环 N 次」功能。
- 循环时，视频与配音**作为一个整体**从块首同步重放。

### 2.5 配音录制
- **录入口径：以「块」为单位**。每次录制选中一块 → 视频从块首按 **1x 原速**播到尾一遍（**不循环**、**录音期间锁定 1x，不可调倍速**）→ 用户对块配音。
- 录制期间**原声照常播放**（用户能听到节奏 / 音乐对准它），**麦克风只录人声**。
- **手动中途停止**即视为录完该块：取录到的实际时长，**在块内靠前对齐**，块内剩余部分静音。不要求必须录满整块。
- **一个视频只保留一条完整配音音轨**，由若干「块片段」**拼接**而成：
  - 每个块片段锚定在它对应块的**绝对起止时间**上（视频时间线）。
  - 对某块重新录制 = **替换**该块对应的片段，其余片段不动。
  - 未录制配音的块区域在回放中即为**静音**。
  - 主页「是否有配音」= 是否至少录过一个片段。

### 2.6 回放（配音 + 原视频一起放）
- **原声 / 配音双音轨，可分别调音量**（两条滑条），并有一个**配音开关**。
  - 配音打开：原声照常放 + 配音叠加（默认配音音量较满）。
  - 配音关闭：仅原声，等同普通播放。
  - 可把原声调到 0，只听众配音。
- **倍速对视频 + 配音整体生效**（二者始终同步）。速率档位：0.5 / 0.6 / 0.7 / 0.8 / 0.9 / 1.0（0.1 步进）。范围当前为 0.5–1.0，架构上允许以后扩到 2.0，但本期不做。
- **循环播放**时：主控（视频）与配音**同步**，每遍循环配音从块首重来。
- 未录配音的段回放：仅原声，无静音剥夺。

### 2.7 手势控制（播放时，仿 B 站 / 爱奇艺客户端）
- **长按屏幕右侧**：暂时切换为 **0.5x 慢速**播放，松手恢复原倍速。
- **长按屏幕左侧**：**2x 倒放**（反向播放），松手恢复。倒放时配音同步倒放 / 或暂停配音（见 5.6）。
- **左右滑动**（横滑，不区分左右半屏）：在屏幕内向右滑 = **向后跳转**（快进），向左滑 = **向前跳转**（快退）。滑动手势显示一个提示条（如「+10s / -10s」），松开后跳转。
- 手势仅在**视频画面区域**、非控件上生效；控件（按钮 / 滑条）的点击与滑动优先，不触发这些手势。
- 正在录制时不触发这类手势（长按 / 滑动被录制按钮与录制态屏蔽）。

### 2.8 全屏与控件显隐
- 播放时**默认全屏**（进入播放器页即全屏），并屏蔽系统状态栏 / 导航栏（`immersive` 模式），**不显示任何设置按钮**。
- **单击屏幕**唤出控件层（透明叠加的按钮 / 滑条 / 时间轴 / 分割点手柄）。此时**系统栏**一并短暂显示。
- 控件层唤出后，**单击屏幕中间**（非控件区域），或**几秒不操作**，控件层自动隐藏，回到纯全屏。
- 控件层的进度条、段选择、音量滑条等只在其显示时可用；隐藏时纯看画面。
- 播放页保持屏幕常亮（`FLAG_KEEP_SCREEN_ON`）。

### 2.9 横竖屏适配
- 视频可能是**横屏**或**竖屏**，都要能适配。
- 处理策略：
  - **画面自适应**：以「内容适配」为主（`RESIZE_MODE_FIT`），视频按自身宽高比缩放至屏幕内，四周留黑边；不强行拉伸。
  - **方向跟随**：默认**跟随设备**（横屏设备 → 横屏画面，竖屏 → 竖屏画面）。由于是全屏播放，横屏视频在竖屏设备上会以「横置画面」呈现，**默认不自动旋转**（避免练舞时频繁转屏）。视频方向以正确显示为优先，不强制某方向。
  - 录制 / 练习的定位与分段**与视频方向无关**（方向只是显示层面）。
  - 若横屏视频被用户手动旋转了屏幕方向，App 允许横屏画面旋转显示（`android:screenOrientation` 不锁死）。

---

## 3. 数据模型

全部落在 App 内部 Room 数据库（`AppDatabase`）。文件存 App 私有目录。

```
Video
  id          : PK
  title       : String          // 标题
  filePath    : String          // 本地文件绝对路径（含下载的网络视频）
  durationMs  : Long            // 视频总时长（毫秒）
  thumbnailPath : String?       // 封面帧路径（提取中间帧）
  sourceType  : LOCAL | NETWORK_DOWNLOADED
  createdAt   : Long

Segment
  id          : PK
  videoId     : FK -> Video
  index       : Int             // 段序号（0..N）
  startMs     : Long            // 该段起点（视频时间线，毫秒）
  endMs       : Long            // 该段终点（毫秒）
  // 段由分割点切分：段 i = [split[i-1], split[i])；段 0 起点为 0

RecordingChunk                     // 一个「块」的配音片段
  id          : PK
  videoId     : FK -> Video
  audioPath   : String           // 录音文件（m4a）路径，App 私有目录
  blockStartMs: Long             // 该块在视频时间线上的起点（= 块首段 startMs）
  blockEndMs  : Long             // 该块在视频时间线上的终点（= 块尾段 endMs）
  recordedMs  : Long             // 实际录音时长（<= blockEndMs - blockStartMs）
  createdAt   : Long
```

**配音音轨是「概念上的一个整体」**：某视频的所有 `RecordingChunk` 按其绝对时间拼接，缺块补静音。**不**在录制时把片段物理拼成一个文件；而是**回放时**按需组合音频源。

---

## 4. 界面结构

```
MainActivity
├── HomeScreen                // 视频库（首页）
│     ├── 视频列表项（标题 / 封面 / 时长 / 有配音标记）
│     └── 悬浮「+」按钮 → BottomSheet：添加本地视频 / 输入 URL
│           └── URL 元信息卡片（标题/时长/封面 + 下载本地按钮）
└── PlayerScreen              // 播放器页（合并界面，容纳：播放/分段/配音/循环）
      ├── 视频画面 (PlayerView)
      ├── 手势层：长按右侧=0.5x / 长按左侧=2x倒放 / 横滑=前进后退（仅画面区域）
      ├── 控件层（单击唤出，单击屏幕中间或超时自动隐藏；默认隐藏=纯全屏）：
      │     ├── 顶部：标题 / 返回 / 系统栏
      │     ├── 时间轴 + 分割点手柄（插入/删除/微调）
      │     ├── 中带：段选择条（连续段构成块）
      │     ├── 控制栏：播放/暂停、循环、倍速选择器、配音开关、音量滑条×2
      │     └── 录制按钮（点击进入录制态，录制中显示计时并可停止）
      └── 全屏处理：immersive 全屏 + 屏幕常亮；横竖屏均以 FIT 自适应

导航：单 Activity + Compose Navigation；点视频 → PlayerScreen。
```

---

## 5. 技术方案

### 5.1 依赖（基于本机已缓存的 Media3 1.5.1）
- `androidx.media3:media3-exoplayer:1.5.1`
- `androidx.media3:media3-ui:1.5.1`
- `androidx.media3:media3-common:1.5.1`
- Room：`androidx.room:room-runtime` / `room-ktx` / `room-compiler`（用 KSP）
- Compose BOM（项目已配）、`material3`、`lifecycle-viewmodel-compose`
- 权限：`RECORD_AUDIO`（录音）、`READ_MEDIA_VISUAL_USER_SELECTED` / `READ_EXTERNAL_STORAGE`（选本地视频，用系统文件选择器则未必需）、`INTERNET`（网络视频）

> 已核对源码：1.5.1 含 `ClippingMediaSource`、`LoopingMediaSource`、`SilenceMediaSource`、`MergingMediaSource`、`ProgressiveMediaSource`。**不含** `OverlayMediaSource`（更晚版本才有），故不依赖它。

### 5.2 播放模型 —— 双 Player 同步

采用**两个 ExoPlayer**，分别负责「视频」与「配音」，以便各自独立调音量：

- **主 Player**：播放视频源（含视频原声轨）。负责输出画面与原声，是**主时钟**。
- **配音 Player**：播放「块配音源」。**audio-only**（无画面），与主 Player **位置对齐同步**。

**配音源的构造**（针对当前选中的「块」，范围 `[blockStart, blockEnd)`）：
1. 取出该视频所有 `RecordingChunk` 中与块相交的片段。
2. 对每个片段：`ClippingMediaSource(audio, chunkStart, chunkEnd)` 裁到块内交集。
3. 用 `SilenceMediaSource` 补上块内未覆盖的静音区间。
4. 用 `MergingMediaSource` 把各片段 + 静音段**按绝对时间对齐**合成一轨，使其时间长度 == 块时长。
5. 整轨包 `LoopingMediaSource`，与主视频块的循环节奏一致。

**同步策略**：
- 主 Player 决定块循环边界。侦听其 `onPositionDiscontinuity` / 位置回卷，确认循环到块首。
- 配音 Player 用同样的 `LoopingMediaSource`，在**每个块循环起点**收到信号后 `seekTo(0)` 并 `play()`。
- 循环内的同步由「整轨与块等长 + 同速倍速」保证，无需逐帧校准（两 Player 倍速参数设为相同值）。
- 块范围改变、配音开关 / 音量改变、倍速改变时，**重建或调整配音源**。

**音量与开关**：
- 主 Player `volume` = 原声音量滑条。
- 配音 Player `volume` = 配音音量滑条。
- 配音开关 = 停止/释放配音 Player，或将配音 Player `volume = 0`。

### 5.3 录音方案
- 用 `MediaRecorder`（AAC → `.m4a`）录制麦克风，输出到 App 私有目录。
- 录音开始时，将**视频整块 @1x 播放**（录音期间锁定倍速为 1.0，不随 UI 倍速）。
- 手动停止 / 到块尾自动停止 → 结束 `MediaRecorder`。
- 用 `MediaExtractor` / `MediaMuxer` 读回录音**实际时长** `recordedMs`。
- 生成 `RecordingChunk(videoId, audioPath, blockStartMs=块首, blockEndMs=块尾, recordedMs, ...)` 存入数据库。若该块已有片段则**替换**。
- 块内 `[recordedMs, blockEnd)` 剩余区间在回放中由 `SilenceMediaSource` 补静音（对应 2.5 的 B1 语义）。

### 5.4 分段 / 分割点
- 前进 / 后退定位到某时刻，时间轴「插入分割点」→ 生成新 `Segment`（重排 index）。
- 「删除」某分割点 → 合并相邻两段。
- 「微调」：对分割点按 ±10ms / ±100ms 步进微调，实时反映到段边界。
- 段选择：连续段范围内起始/结束段两个手柄，选中即组成块。

### 5.5 手势控制（速度 / 跳转）
统一用一个全屏 `Modifier.pointerInput` 手势层，置于视频画面之上、控件层之下；**手势层只响应非控件区域**。
- **长按右侧** → `playbackSpeed = 0.5`，抬起恢复。
- **长按左侧 → 2x 倒放（模拟）**：
  - ExoPlayer / Media3 的 `setPlaybackSpeed` **不支持负速度**（真实倒放需逐帧解码 + 反向合成，开销大，且本版本无此 API）。因此「2x 倒放」采用**用户确认接受的模拟倒放**：长按期间正常播放暂停，按 2x 速率**定时向回 seek**（例如每 500ms `seekTo(position - 1000ms)`），视觉与听觉上都呈倒放效果。
  - 倒放期间**配音暂停**（配音 Player `pause()`），避免配音与倒放画面脱节；恢复后配音随主控重新同步。
- **横滑**：用 `detectHorizontalDragGestures`（或在水平拖动距离阈值后触发），按拖动距离比例计算跳转量并显示「±N 秒」提示条；松开或达阈值时 `seekTo`。向左为快退、向右为快进。

> 注：手势层的横滑 / 长按将**避开**控件（按钮、滑条、时间轴），控件自身的触摸不落入手势层，避免冲突。录制态时手势层整体禁用。

### 5.6 全屏 / 控件显隐 / 屏幕常亮
- 进入 `PlayerScreen` 即进入**沉浸式全屏**：`WindowInsetsControllerCompat` 隐藏状态栏与导航栏（`BEHAVIOR_SHOW_TRANSIENT_BARS_BY_SWIPE`），并 `FLAG_KEEP_SCREEN_ON`。
- 用 Compose 状态 `controlsVisible` 管理控件层显隐：单击画面 → `visible=true`；单击画面中间（非控件）或 3 秒无操作 → `visible=false`。
- `visible` 时系统栏临时显示，隐藏时同步隐藏（`hideSystemBars` 由 `visible` 驱动）。
- 控件层各项（时间轴 / 段选择 / 音量 / 倍速 / 分割点 / 录制）只在 `visible` 时可交互。

### 5.7 横竖屏与方向
- `PlayerView` 使用 `PlayerView.RESIZE_MODE_FIT`（`resizeMode = aspect_ratio`），视频按内容宽高比缩放，留黑边，不拉伸。
- 方向：默认**跟随设备**（不把 `screenOrientation` 锁死）。由于全屏 + FIT，竖屏设备上的横屏视频以横置画面呈现，不会自动旋转成竖屏查看（避免翻屏干扰练习）；用户仍可通过系统旋转方向在横竖屏间切换设备，App 正常适配两种方向。
- 分段 / 录制 / 块定义**与方向无关**（仅显示层），方向上不存储任何业务状态。

---

## 6. 关键边界与已定决策（供评审）

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 录音时原声 | 照常播放，麦克风只录人声 |
| 2 | 配音与循环对齐 | 配音随块循环，每遍从头；按段（块）锚定 |
| 3 | 配音粒度 | 与「块」绑定；一个视频只保留一条拼接音轨 |
| 4 | 倍速范围 | 0.5–1.0，0.1 步进，对视频+配音整体生效 |
| 5 | 循环 | 仅无限循环，无 N 次 |
| 6 | 分割点 | 可插 / 可删 / 可 ± 毫秒微调 |
| 7 | 网络视频 | 显示元信息 + 下载到本地，原样保存，参与分段配音 |
| 8 | 界面 | 合并为一个播放器页；主页「+」弹本地/URL |
| 9 | 录音倍速 | 锁定 1x 原速 |
| 10 | 手动停止录音 | 取录到时长，块内靠前对齐，剩余静音 |
| 11 | 音量 | 原声 / 配音独立滑条 + 配音开关 |
| 12 | 手势 | 长按右侧=0.5x、长按左侧=2x倒放（模拟）、横滑=前进后退；录制态禁用 |
| 13 | 全屏 | 默认沉浸式全屏隐藏控件，单击唤出，单击中间或超时隐藏；屏幕常亮 |
| 14 | 横竖屏 | 都适配；FIT 缩放留黑边，方向跟随设备，不锁死、不自动旋转 |
| 15 | 倍速档位（补充） | 常规 0.5–1.0 步进 0.1；长按右侧临时 0.5x、长按左侧倒放为独立手势、非档位 |
| 16 | 倒放实现 | 模拟倒放（定时回 seek，配音暂停），无逐帧真倒放；用户接受 |
| 17 | 控件层超时 | 3 秒无操作自动隐藏；单击屏幕中间也可手动隐藏 |

---

## 7. 里程碑（建议实现顺序）

1. **骨架**：依赖与工程配置、导航、主页视频库（本地添加）。
2. **播放器基础**：主 Player 播放视频、时间轴、进度、倍速、循环、全屏与控件显隐。
3. **分段**：分割点增删与微调、段模型、块选择。
4. **手势**：长按右侧 0.5x、长按左侧 2x 模拟倒放、横滑前进后退（含提示条）；录制态屏蔽。
5. **录音**：`MediaRecorder`、块录音流程、`RecordingChunk` 存储、时长测量。
6. **配音回放**：配音 Player、双 Player 同步、静音补位、音量与开关。
7. **网络视频**：URL 元信息、下载到本地。
8. **打磨**：封面提取、横竖屏适配、中文文案、UI 风格统一。

---

*本文件为定稿方案（功能与技术均经用户确认）。此后按此实现。*
